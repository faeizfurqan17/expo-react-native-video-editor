{
  "name": "expo-react-native-video-editor-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "build": "yarn workspace @anthropic/react-native-video-editor build",
    "example": "yarn workspace video-editor-example start",
    "lint": "yarn workspace @anthropic/react-native-video-editor lint",
    "typecheck": "yarn workspace @anthropic/react-native-video-editor typecheck"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
